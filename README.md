# Cerberus
The complete Cerberus product as one compose project: `cerberus-api` (the policy gateway) + `cerberus-fusion-worker` (the bundled openfusion fork that executes panel+judge). Source lives in `labs/cerberus`; this tree is **compose + config + file-secrets references only**, per the gateway convention.
